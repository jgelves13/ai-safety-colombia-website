---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';
import { t, type Locale } from '../i18n/ui';
import type { AisEvent } from '../types';
import { typeColor } from '../utils/eventTypes.js';

interface Props {
  event: AisEvent;
  locale: Locale;
}

const { event, locale } = Astro.props;

const flyers = import.meta.glob<ImageMetadata>('../assets/events/*', {
  eager: true,
  import: 'default',
});
const flyer = flyers[`../assets/events/${event.flyer}`];

const color = typeColor(event.type);
const tagLabel =
  t(locale, `events.type.${event.type}` as Parameters<typeof t>[1]) || event.type;
const title = locale === 'es' ? event.titleEs : event.titleEn;
const desc = locale === 'es' ? event.descEs : event.descEn;
const venue = locale === 'es' ? event.venueEs : event.venueEn;
const why = locale === 'es' ? event.whyEs : event.whyEn;

const dateFmt = new Intl.DateTimeFormat(locale === 'es' ? 'es-CO' : 'en-US', {
  day: 'numeric', month: 'short', year: 'numeric',
});
const dateLabel = dateFmt.format(new Date(event.date + 'T00:00:00-05:00'));
const metaTail = [event.time, venue].filter(Boolean).join(' · ');
const alt = `${title}. ${tagLabel}, ${dateLabel}.`;
---

<article class="event-card" data-event-type={event.type}>
  <a class="event-flyer" href={event.instagramUrl} target="_blank" rel="noopener noreferrer" aria-label={title}>
    {flyer && (
      <Image
        src={flyer}
        alt={alt}
        widths={[260, 520, 780]}
        sizes="(max-width: 768px) 94vw, 260px"
        format="webp"
        quality={90}
        loading="lazy"
      />
    )}
  </a>
  <div class="event-body">
    <span class="event-tag" style={`color:${color}`}>{tagLabel}</span>
    <h3 class="event-title">{title}</h3>
    <p class="event-meta">
      <time datetime={event.date}>{dateLabel}</time>{metaTail && <span> · {metaTail}</span>}
    </p>
    <p class="event-desc">{desc}</p>
    <p class="event-why">{why}</p>
    <a class="event-link" href={event.instagramUrl} target="_blank" rel="noopener noreferrer">
      {t(locale, 'events.viewInstagram')}
    </a>
  </div>
</article>

<style>
  .event-card {
    display: flex;
    gap: 1.25rem;
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 1.25rem;
    background: var(--card-surface);
    transition: border-color 0.2s, box-shadow 0.2s, transform 0.2s;
  }

  .event-card:hover {
    border-color: var(--accent-green);
    box-shadow: 0 6px 24px rgba(16, 16, 16, 0.06);
    transform: translateY(-2px);
  }

  .event-flyer {
    width: 260px;
    flex: none;
    aspect-ratio: 3 / 4;
    border-radius: 12px;
    overflow: hidden;
    display: block;
    background: #F4F6F4;
    border: 1px solid var(--border);
  }

  /* contain (not cover) so the whole flyer is visible, never cropped */
  .event-flyer :global(img) {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .event-body {
    display: flex;
    flex-direction: column;
    min-width: 0;
  }

  .event-tag {
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    font-family: var(--font-mono);
  }

  .event-title {
    font-size: 1.0625rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    color: var(--text-primary);
    margin: 0.25rem 0;
  }

  .event-meta {
    font-size: 0.8125rem;
    color: var(--text-secondary);
  }

  .event-meta time {
    font-family: var(--font-mono);
  }

  .event-desc {
    font-size: 0.8125rem;
    color: var(--text-secondary);
    line-height: 1.5;
    margin-top: 0.375rem;
  }

  .event-why {
    font-size: 0.8125rem;
    color: var(--text-primary);
    line-height: 1.5;
    margin-top: 0.5rem;
    padding-left: 0.625rem;
    border-left: 2px solid var(--accent-green);
  }

  .event-link {
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--green-text);
    margin-top: 0.625rem;
  }

  @media (max-width: 768px) {
    .event-card {
      flex-direction: column;
    }
    .event-flyer {
      width: 100%;
      aspect-ratio: auto;
      max-width: 360px;
      align-self: center;
    }
    .event-flyer :global(img) {
      height: auto;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .event-card,
    .event-card:hover {
      transition: none;
      transform: none;
    }
  }
</style>
