# Project Notes

## Alternative hackathon landing page

- Use `/alternative-landing-page/` for the standalone hackathon landing direction.
- The page is implemented in `src/pages/alternative-landing-page/index.astro`, `src/components/AlternativeHackathonLanding.astro`, and `src/data/alternativeHackathonLanding.ts`.
- To preview it locally, run `npm run dev -- --host 127.0.0.1 --port 4321` and open `http://127.0.0.1:4321/alternative-landing-page/`.
- The hero must make both `Bogotá Hub` and `AI Safety Hackathon` immediately visible. Do not bury Bogotá in small metadata or secondary cards.
- Keep the primary CTA visible in the hero, with a secondary details CTA that jumps to the detail cards.
- The online option should remain visible but quieter than the Bogotá hub.
- The detail cards should clarify logistics, format, and deliverable expectations.
